{
  "name": "onward",
  "private": true,
  "version": "0.1.0",
  "description": "Onward — India salary, tax & offer-letter decoder",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm test --workspace @onward/engine",
    "dev": "next dev apps/web -p 3000",
    "build": "npm run build --workspace @onward/web"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
